# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
dist/
*.egg-info/
*.egg
.venv/
venv/

# Node.js / TypeScript
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
*.tsbuildinfo
.npm

# AWS CDK
cdk.out/
.cdk.staging/
cdk.context.json
execution-zones/*/cdk/cdk.config.*.json
execution-zones/*/cdk/node_modules/
execution-zones/*/cdk/cdk.out/
verification-zones/*/cdk/cdk.config.*.json
verification-zones/*/cdk/node_modules/
verification-zones/*/cdk/cdk.out/

# Environment variables
.env
.env.local
.env.*.local

# IDE / OS
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store
Thumbs.db

# Local MCP config
.mcp.json

# Logs
*.log
logs/

# Testing
coverage/
*.cover
.pytest_cache/
.coverage
.coverage.*
htmlcov/
coverage.xml

# Temporary files
*.tmp
*.temp
.cache/

# Lambda deployment packages
*.zip

# Execution Agent: copied docs/ at deploy time (Pattern 1); do not commit copy
execution-zones/execution-agent/src/docs/

# Agent zone Dockerfiles must stay tracked for CDK DockerImageAsset (clone must include src/Dockerfile).
# If a developer’s global excludes file lists "Dockerfile", uncommitted copies are skipped until: git add -f <path>
