# slack-ai-app Development Guidelines

Auto-generated from all feature plans. Last updated: 2025-12-06

## Active Technologies
- Python 3.11（Execution / Verification エージェント）、TypeScript 5.x（CDK） (014-a2a-file-to-slack)
- 既存の DynamoDB / Secrets Manager は変更なし。ファイルは A2A レスポンス内で base64 等で運び、永続化しない。 (014-a2a-file-to-slack)
- なし（ファイルは A2A レスポンスでメモリ上のみ受け渡し。永続化は本機能スコープ外） (014-a2a-file-to-slack)
- TypeScript (CDK, Node 18+), Python 3.11+ (Lambda/Agent runtimes) + aws-cdk-lib, AWS AgentCore Runtime (Bedrock AgentCore), existing ECR/Agent constructs (015-agentcore-a2a-migration)
- DynamoDB (token, dedupe, existence check, whitelist, rate limit), Secrets Manager (Slack credentials) — preserved; no schema changes (015-agentcore-a2a-migration)
- TypeScript (CDK, Node 18+), Python 3.11+ (Lambda) + aws-cdk-lib, boto3 (SQS, bedrock-agentcore), 既存 Verification Agent Runtime (016-async-agentcore-invocation)
- DynamoDB（既存: dedupe, whitelist, rate limit, token）— 変更なし。SQS キュー（新規: agent-invocation-request）で実行リクエストを渡す。 (016-async-agentcore-invocation)
- TypeScript (CDK, Node 18+), Python 3.11 (Lambda) + 既存 aws-cdk-lib, slack_sdk (WebClient), boto3 — 追加依存なし (017-validation-zone-echo)
- なし（エコー内容はリクエストからその場で生成） (017-validation-zone-echo)
- Python 3.11 (Verification Agent, SlackEventHandler Lambda), TypeScript (CDK, Node 18+) + 既存 aws-cdk-lib, bedrock_agentcore.runtime, slack_sdk, boto3 — 追加依存なし (018-echo-at-agentcore-runtime)
- なし（エコーはリクエストコンテキストからその場で生成） (018-echo-at-agentcore-runtime)
- Python 3.11 (agents), TypeScript 5.x (CDK) + strands-agents[a2a]~=1.25.0, bedrock-agentcore, boto3, openpyxl, python-docx, python-pptx, matplotlib, aws-cdk-lib (027-slack-file-generation-best-practices)
- DynamoDB, S3（既存）、変更なし (027-slack-file-generation-best-practices)
- Python 3.11 (Verification Agent, Slack Poster Lambda), TypeScript 5.x (CDK) + boto3, FastAPI, uvicorn, slack-sdk, aws-cdk-lib (028-s3-large-file-transfer)
- S3 (file-exchange バケット), 既存 `attachments/` に加え `generated_files/` プレフィックス追加 (028-s3-large-file-transfer)
- TypeScript 5.x (cdk/), Node.js 18+ + aws-cdk-lib 2.215.x, constructs ^10.x, Jest (tests), zod (config validation) (029-cdk-logging-error-handling)
- N/A (infrastructure definitions only; runtime state is in AWS) (029-cdk-logging-error-handling)

- Python 3.11+ for Lambda functions, TypeScript for AWS CDK infrastructure + AWS CDK, boto3 (Bedrock SDK), slack-sdk (Python), AWS Lambda runtime, requests/httpx for file downloads, PyPDF2/pdfplumber for PDF extraction, python-docx for DOCX extraction (004-slack-attachments)

## Project Structure

```text
src/
tests/
```

## Commands

cd src [ONLY COMMANDS FOR ACTIVE TECHNOLOGIES][ONLY COMMANDS FOR ACTIVE TECHNOLOGIES] pytest [ONLY COMMANDS FOR ACTIVE TECHNOLOGIES][ONLY COMMANDS FOR ACTIVE TECHNOLOGIES] ruff check .

## Code Style

Python 3.11+ for Lambda functions, TypeScript for AWS CDK infrastructure: Follow standard conventions

## Recent Changes
- 029-cdk-logging-error-handling: Added TypeScript 5.x (cdk/), Node.js 18+ + aws-cdk-lib 2.215.x, constructs ^10.x, Jest (tests), zod (config validation)
- 028-s3-large-file-transfer: Added Python 3.11 (Verification Agent, Slack Poster Lambda), TypeScript 5.x (CDK) + boto3, FastAPI, uvicorn, slack-sdk, aws-cdk-lib
- 027-slack-file-generation-best-practices: Added Python 3.11 (agents), TypeScript 5.x (CDK) + strands-agents[a2a]~=1.25.0, bedrock-agentcore, boto3, openpyxl, python-docx, python-pptx, matplotlib, aws-cdk-lib


<!-- MANUAL ADDITIONS START -->
<!-- MANUAL ADDITIONS END -->
